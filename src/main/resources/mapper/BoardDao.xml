<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gyojincompany.profile.dao.BoardDao">
	
	<!-- 글쓰기 -->
	<insert id="writeDao">
		INSERT INTO profileboard(bnum, bid, bname, btitle, bcontent, bhit)
			VALUES (profileboard_seq.nextval, #{param1}, #{param2}, #{param3}, #{param4}, 0) 
	</insert>
	
	<!-- 모든 글 가져오기 -->
	<select id="listDao" resultType="com.gyojincompany.profile.dto.BoardDto">
		SELECT * FROM profileboard ORDER BY bnum DESC
	</select>
</mapper>    